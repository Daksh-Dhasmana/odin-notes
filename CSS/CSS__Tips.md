# Some Useful CSS tips

## 1.rgba()
- `rgba()` affects "only" the applied element not it's grandchildren and children.
- `background-color: rgba(0, 0, 0, 0.5);`, we are telling browser that "Paint only the floor of this box with 50% transparent black paint."